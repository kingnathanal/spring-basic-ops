apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Chart.Name }}
spec:
  hosts:
  - {{ $url := print .Chart.Name ".app.williambrittonjr.com" }}{{ $url }}
  http:
  - route:
    - destination:
        host: {{ $url2 := print .Chart.Name .Values.Namespace ".svc.cluster.local" }}{{ $url2 }}